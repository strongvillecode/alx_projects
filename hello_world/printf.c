#include <stdio.h>
/**
 * main - entry point for the programme
 *
 * Return : 0 if errors, non zeros if no errors
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art");
		return (0);
}
